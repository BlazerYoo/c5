#!/usr/bin/env python3
# Placeholder Chrome Native Messaging Host.
# Claude Code expects ~/.claude/chrome/chrome-native-host to exist.
# The MCP server does NOT invoke this — it watches the socket directory directly.
import sys

sys.exit(0)
